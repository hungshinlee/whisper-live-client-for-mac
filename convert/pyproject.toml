[project]
name = "whisper-mlx-convert"
version = "0.1.0"
description = "Convert Whisper models to MLX format"
requires-python = ">=3.11"
dependencies = [
    "mlx",
    "mlx-whisper",
    "numpy",
    "safetensors",
    "huggingface_hub",
]
