[project]
name = "whisper-mlx-client"
version = "0.1.0"
description = "MLX Whisper client for real-time speech transcription (Apple Silicon GPU)"
requires-python = ">=3.11"
dependencies = [
    "mlx-whisper",
    "pyaudio",
    "numpy",
]
